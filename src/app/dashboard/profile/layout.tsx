import { ReactNode } from "react";

export const metadata = {
  title: "Profile - ValidChat",
  description: "Update your profile information.",
};

export default function ProfileLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
